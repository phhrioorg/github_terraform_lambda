This module will create the ecr repository in AWS.

The parameters to the module are 
  ecr_name         = The name for this container registry
  image_mutability = image_mutability
  tags             = AWS tags to use